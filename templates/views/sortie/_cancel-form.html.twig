{% form_theme cancelForm 'views/form/form-theme-sortie.html.twig' %}

  {{ form_start(cancelForm) }}
  {{ form_start(cancelForm) }}
  {{ form_row(cancelForm.infosSortie) }}
    <button  class="transition w-full font-medium text-neutral-200 bg-neutral-800 pt-2 pb-1 mt-1 rounded-full text-sm">
        Annuler la sortie
    </button>
  {{ form_end(cancelForm) }}

